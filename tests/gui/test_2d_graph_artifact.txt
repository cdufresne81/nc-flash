# Test: 2D table graph toggle - window should shrink back to original size
# This test verifies that opening/closing the graph on a 2D table
# properly restores the window to its original width (no blank space artifact)

load_rom examples/lf9veb.bin

# Use a 2D table from the ROM definition
open_table "APP to TP Desired - Max"
wait 300

# Store the initial window width before opening graph
store_width

# Screenshot table without graph
screenshot 2d_graph_01_initial table

# Open graph first time
open_graph
wait 500
screenshot 2d_graph_02_with_graph table

# Close graph - window should shrink back to original width
close_graph
wait 300
screenshot 2d_graph_03_closed table

# Assert window width is restored to original
assert_width_restored

# Test toggle cycle again to make sure it's repeatable
open_graph
wait 300
close_graph
wait 300
assert_width_restored

# Final screenshot to confirm no artifact
screenshot 2d_graph_04_final table

close_table
