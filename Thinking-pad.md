# Thinking pad - **DO NOT TAKE ACTION UNLESS I ASK YOU TOO**

- Can we reduce the white border around the graph a little ?
- When comparing to ROM from the same project we should skip most of the lenghty validations.
- Improve project management UI.
- Create new ROM XML defintion/metadata when definition is unavailable.

# FUN stuff
- Share a ROM.

# ECU tools
- All flashing options should be disabled by default.
- Flashing options should be availble if we have a valid connection with the flash unit, eg: something like tactrix open port.
- Can we have an indication that the OBD2 flashing unit is connected ? Can it tell if the car or ECU is connected, running and such, low battery, etc ?
- A Dynamic flash should only be allowed if we have all the files in place to do it.
- We should not be able to Dynamic flash a patched ROM over a ROM that was never patched. eg: Patch ROM, Full flash patch ROM, next iteration can do dynamic Flash.  
- Asked if I want a Full flash or a Dynamic flash.
- I want to see the flash counter in the flash window. What about DTC and VIN ?
- The patch tools should be have a wizard, select rom, select patch, show the patching result (did it worked), indiciate where the patched file is.