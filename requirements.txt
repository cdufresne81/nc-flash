# GUI Framework
PySide6>=6.10.0,<7.0.0

# Data manipulation and visualization
numpy>=2.2.0,<3.0.0
# Classic graph renderer: fallback when no GPU adapter is available
matplotlib>=3.10.0,<4.0.0
# GPU graph renderer (WebGPU). Exact pins: pre-1.0 APIs churn between releases.
# wgpu 0.32 needs Python >= 3.11; an older interpreter (e.g. an existing 3.10
# venv) skips them and the graph falls back to the classic renderer.
pygfx==0.17.0; python_version >= "3.11"
wgpu==0.32.0; python_version >= "3.11"
rendercanvas==2.7.2; python_version >= "3.11"

# XML parsing for ROM definitions
lxml>=6.0.0,<7.0.0

# Safe expression evaluation for scaling formulas
simpleeval>=1.0.0,<2.0.0

# MCP server for AI assistant access
mcp>=1.26.0,<2.0.0

# mDNS/Zeroconf auto-discovery of the WiCAN adapter (optional at runtime: the
# app degrades to manual IP entry if it is missing)
zeroconf>=0.130.0,<1.0.0
