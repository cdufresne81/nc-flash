# Session logs
logs/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
venv-windows/
env/
ENV/

# IDE
.vscode/
.idea/
# Machine-local Claude Code config (permission grants, MCP toggles), plus the
# per-feature planning/design docs (AI working notes; the CHANGELOG + code are the
# durable record). Shared .claude/ tooling — hooks, commands, notes — stays tracked.
.claude/settings.json
.claude/settings.local.json
.claude/plans/
*.swp
*.swo
*~

# PyInstaller
*.manifest

# Inno Setup output
Output/

# OS
.DS_Store
Thumbs.db
desktop.ini
nul

# ROM files (for testing, don't commit actual ROM files)
*.bin
*.hex
*.rom
!examples/*.bin

# Logs
*.log

# Testing
.pytest_cache/
.coverage
htmlcov/
coverage.xml
.tox/

# Debug scripts (not launchers)
debug_*.py
*_debug.py
*_debug.log

# Debug GUI test scripts (one-off debugging, not real tests)
tests/gui/debug_*.txt

# Test screenshots (auto-generated, don't commit)
docs/screenshots/*.png
docs/screenshots/*.jpg
docs/screenshots/*.jpeg

# Personal notes
Thinking-pad.md
thinking-pad.md

# Projects directory (user projects, don't commit)
projects/*
!projects/.gitkeep

# Workspace state file (written by app, read by MCP server)
workspace.json

# Private security module (seed-to-key algorithm + SBL blobs)
# Lives in separate private repo: nc-flash-secure
src/ecu/_secure/_security.py
src/ecu/_secure/_sbl.py
src/ecu/_secure/__init__.py


# Third-party bug-report attachments + generated correction deliverables
# (LF9KT investigation, Jul 2026 — not ours to publish)
/LF9KT001.xml
/LF9KT001_corrected.xml
/LF9KT001_correction_audit.md
